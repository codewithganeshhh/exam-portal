version: '3.8'

services:
  # Express Backend API
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: exam_portal_server
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://ganeshplays700_db_user:0A38DNGy00iWVStB@cluster0.5atdutm.mongodb.net/exam_portal?retryWrites=true&w=majority
      - JWT_SECRET=f98a2e4c91d7b68a5c31e0f4928d3e7b1a6c5f89e240b7194d38c6a2e5f1b09d84e72a19b5c30f4e6d28a7b9c1f0e4d25a8b7c3e91f0a6b4d8c2e5f7a1b3c9d4
      - CORS_ORIGIN=*
    networks:
      - exam_network

  # React Vite Frontend Client
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://localhost:5000/api
    container_name: exam_portal_client
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - server
    networks:
      - exam_network

networks:
  exam_network:
    driver: bridge
